# 原子任务执行工作流程

## 概述
本文档定义了一个完整的工作流程，用于将用户需求分解为原子任务，之后执行每个任务，完成后进行git提交和日志记录。

## 工作流程步骤
首先分析代码，将分析结果写入agent/global_log.md

### 第一步：任务划分
1. **输入**：用户提供的代码或需求描述
2. **使用**：`task_planning_prompt.md` 中的prompt模板
3. **输出**：`backlog.json` 文件

### 第二步：任务执行
对backlog.json中的每个任务按以下流程执行：
1. 查看任务ID、标题和描述
2. 检查依赖任务是否已完成
3. 执行任务
4. 验证任务完成情况
5. 中途创建md、test等无关文件即使删除

### 第三步：Git提交和日志记录

#### 3.1 Git提交规则
每个任务完成后必须进行git提交：

```bash
# 提交消息格式
git add .
git commit -m "Task-{task_id}: {task_title}"
```

#### 3.2 日志记录规则
在本地创建 `agent` 文件夹，用于记录日志。每次修改代码后，都要保持记录，按照以下格式记录：

```
## TASK-001: 定义数据库模型
- **Summary**: 修改了什么文件，添加、删除了什么。
- **Errors**: 遇到什么错误（若没有则为None）。
- **Context**: 用户有什么反馈或纠正（若没有则None）。
```

每个原子任务开始前，都去阅读这个日志作为上下文。

## 文件结构

```
project_root/
├── backlog.json                 # 任务清单
├── agent/                       # 日志文件夹
│   └── global_log.md             # 任务执行日志
├── task_planning_prompt.md      # 任务划分prompt模板
├── task_workflow.md             # 本工作流程文档
└── src/                         # 源代码目录
```

## backlog.json 格式

```json
[
  {
    "id": "TASK-001",
    "title": "简短标题",
    "type": "任务类型",
    "description": "详细说明：要做什么，逻辑重点。",
    "test_command": "验证此任务的命令",
    "context_needed": ["需要关注的前置函数或模型名称"]
  }
]
```

## 执行检查清单

### 任务执行前检查
- [ ] 理解任务需求和描述
- [ ] 阅读 agent/global_log.md 获取上下文
- [ ] 了解验证标准

### 任务执行中检查
- [ ] 按照任务描述进行修改
- [ ] 保持代码风格一致

### 任务完成后检查
- [ ] 验证标准全部满足
- [ ] 执行git提交
- [ ] 更新 agent/global_log.md 日志

## 最佳实践

1. **原子性**：确保每个任务可以独立完成和验证
2. **可追溯性**：通过git提交和日志记录保持完整的执行历史
3. **上下文保持**：在日志中记录足够的上下文信息供后续任务参考
4. **及时提交**：每个任务完成后立即提交，避免积累大量更改
5. **验证优先**：确保任务质量，不满足验证标准不提交